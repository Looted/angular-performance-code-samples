import { MultiplyAndDividePipe } from './multiply-and-divide.pipe';

describe('MultiplyAndDividePipe', () => {
  it('create an instance', () => {
    const pipe = new MultiplyAndDividePipe();
    expect(pipe).toBeTruthy();
  });
});
